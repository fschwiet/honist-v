interface:
  display_name: 'Retire Provisional Documents'
  short_description: 'Preserve rationale, then retire provisional docs'
  default_prompt: "Use $retire-provisional-documents to safely retire this repository's provisional documentation."
policy:
  allow_implicit_invocation: false
